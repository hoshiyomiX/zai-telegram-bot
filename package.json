{
  "name": "terabox-telegram-bot",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Terabox to Telegram bot",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
